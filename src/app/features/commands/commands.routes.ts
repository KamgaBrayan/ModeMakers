import { Routes } from '@angular/router';
import { CommandsComponent } from './commands.component';

export const COMMANDS_ROUTES: Routes = [
  {
    path: '',
    component: CommandsComponent
  }
];
